export class Entry {
	id: string;
	groupId: string;
	text: string;
	created: Date;
	lastEdited: Date;
	indentLevel: number;
	quoted?: string;

	constructor(text) {
		id: 
	}
	setText() {}
	setIndent() {}
	addChild() {} // TODO: Also remember to have them pass back errors
}